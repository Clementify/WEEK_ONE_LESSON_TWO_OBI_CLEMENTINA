//  Nest "if" statements 
// ● Declare two variables, num1 and num2, and assign them numeric values.
// ● Write an outer "if" statement to check if num1 is greater than 0.
// ● Inside the "if" block, write an inner "if" statement to check if num2 is greater
// than 0.
// ● If both conditions are true, log a message stating that both numbers are
// positive.

const num1 = 4
const num2 = 2

if (num1 > 0) {
  if (num2 > 0) {
    console.log('Both numbers are positive')
  }
}